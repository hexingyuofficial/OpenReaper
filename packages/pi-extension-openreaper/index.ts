/**
 * Pi auto-discovery entry (`agentDir/extensions/*.ts`).
 * Canonical implementation: ./openreaper-extension.mjs (also passed as `--extension`).
 */
export { default } from "./openreaper-extension.mjs";
