{
  "name": "openreaper",
  "version": "0.0.0",
  "private": true,
  "description": "Clean foundation repo for the OpenReaper verified REAPER agent runtime.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm run check:layout && npm run check:tool-abi",
    "check:layout": "node scripts/check-repo-layout.mjs",
    "check:tool-abi": "node scripts/check-tool-abi.mjs",
    "check:layer": "node scripts/check-layer-scope.mjs",
    "build": "npm run check:layout && npm run check:tool-abi"
  }
}
