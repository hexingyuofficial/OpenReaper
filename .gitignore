.DS_Store
node_modules/
dist/
build/
coverage/
.env
.env.*
*.log
*.tsbuildinfo

# Streetlight local runtime
.streetlight/
queue/
tmp/
# Personalized MCP client config snippets written by `npm run setup`.
# Each clone generates its own absolute-path configs in here; never commit.
setup-out/
# Separate nested project; keep it out of Streetlight commits.
style-memory-mcp/

# REAPER test projects (the bridge lives in reaper/, but user RPP projects do not)
# Anything under reaper/ except the bridge script + packs/ is local-only.
reaper/*
!reaper/streetlight_bridge.lua
!reaper/packs/
!reaper/packs/**
!reaper/.gitkeep
# Re-ignore Finder metadata that `!reaper/packs/**` would otherwise re-include.
reaper/**/.DS_Store

# REAPER artifacts wherever they appear
*.RPP
*.rpp-bak
*.reapeaks
